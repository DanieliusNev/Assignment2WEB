import React from 'react';

const Navigation = () => {
  return (
    <div
      style={{
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        width: '200px',
        height: '100vh',
        backgroundColor: '#f0f0f0'
      }}
    >
      <p>Navigation</p>
      {/* You can also place links here if necessary */}
    </div>
  );
};

export default Navigation;
